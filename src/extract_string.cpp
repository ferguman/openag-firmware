#include "Arduino.h"
#include <types.h>
#include <pair.h>
#include <test.h>

//Internal functions

//Scan the input string and test that it contains a string starting at the position indicated
//by cur_pos.
//If a string is not found then return 0.
//
/*
int exract_string(String *str, unsigned int cur_pos) {

   //All strings must start with ".
   if (str->charAt(cur_pos) == 34) {
      return extract_string_iter(*str, cur_pos+1) {
   } else {
      return 0;
   }
}

int exract_string_iter(String *str, unsigned int cur_pos) {



}
*/

